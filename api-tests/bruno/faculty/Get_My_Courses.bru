meta {
  name: Get My Courses
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/faculty/courses
  body: none
  auth: inherit
}

tests {
  test("should return 200 OK", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
