meta {
  name: Get Profile
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/student/profile
  body: none
  auth: inherit
}

tests {
  test("should return 200 OK", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
