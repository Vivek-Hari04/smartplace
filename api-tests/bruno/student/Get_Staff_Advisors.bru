meta {
  name: Get Staff Advisors
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/student/advisors
  body: none
  auth: inherit
}

tests {
  test("should return 200 OK", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
